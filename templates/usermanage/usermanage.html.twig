{% extends 'base.html.twig' %}

{% block body %}

    <h3 style="display: inline;">Bienvenue {{ app.user.username }}</h3>&nbsp&nbsp&nbsp<a href="{{ path('app_logout') }}">Déconnexion</a>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<a href="{{ path('home') }}">
        Accueil
    </a>
    <br/>
    <br/>
    <br/>
    <form method="post" action="{{ path('search_user') }}">
        <input type="search" placeholder="Recherche" name="motcle" style="width: 100%;">
        <input type="submit" value="Rechercher" style="float: right;">
    </form>
    <br/>   
    <table style="border: 1px solid #333; font-size: 1.5em; width: 100%;">
        <thead style="background-color: black; color:#fff;">
            <tr>
                <th colspan="3">Gestion des utilisateurs</th>
            </tr>
            <tr>
                <td style="text-align: center;">Id</td>
                <td style="text-align: center;">Username</td>
            </tr>   
        </thead>
        <tbody>
            {% for user in users %}
            <tr>
                <td style="border: 1px solid #333; background-color: silver; width: 20%; text-align: center;">{{ user.id }}</td>
                <td style="border: 1px solid #333; background-color: silver; width: 60%; text-align: center;">{{ user.username }}</td>
                <td style="border: 1px solid #333; width: 20%; text-align: center;"><a href="{{ path('delete_user', {id : user.id}) }}">Supprimer</a></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}

